═══════════════════════════════════════════════════════════════
🚀 COPY & PASTE คำสั่งเหล่านี้ใน Terminal แล้วกด Enter
═══════════════════════════════════════════════════════════════

# 1. ไปที่ project directory
cd "/Users/patiphantubhuang/Documents/NITI V3 AI Designsystem"

# 2. Push ขึ้น GitHub (จะถาม username และ password/token)
git push -u origin main

# 3. ติดตั้ง Vercel CLI (ครั้งแรกเท่านั้น)
npm install -g vercel
# หรือถ้า permission denied:
sudo npm install -g vercel

# 4. Login Vercel (ครั้งแรกเท่านั้น)
vercel login

# 5. Deploy
vercel --prod

═══════════════════════════════════════════════════════════════
📝 หมายเหตุ:
═══════════════════════════════════════════════════════════════

1. GitHub Authentication:
   - Username: patiphantubhuang-wq
   - Password: ใช้ Personal Access Token (ไม่ใช่ password)
   - สร้าง token ที่: https://github.com/settings/tokens
   - เลือก scope: repo

2. Vercel Login:
   - ครั้งแรกจะเปิด browser ให้ login

3. หลัง Deploy:
   - Vercel จะแสดง URL สำหรับเข้าถึง Storybook

═══════════════════════════════════════════════════════════════
